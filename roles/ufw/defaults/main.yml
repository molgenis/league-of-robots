---
# Firewall default poorten
firewall_allowed_ports:
  - 22

# IP dat altijd SSH mag gebruiken
firewall_admin_ip:

# interne interfaces die volledig verkeer mogen hebben
firewall_internal_interfaces:
  #- enywhere
  - enp3s0
